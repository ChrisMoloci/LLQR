function encodeData() {
    // -- 1. Segment and encode the data --

    // -- 2. Consolidate encoded segments into a singular stream --

    // -- 3. Return the encoded data --
}

export default encodeData;